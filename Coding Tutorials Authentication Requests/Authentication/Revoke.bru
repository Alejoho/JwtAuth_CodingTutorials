meta {
  name: Revoke
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/authentication/revoke
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

script:post-response {
  // bru.setEnvVar("accessToken","");
  // bru.setEnvVar("refreshToken","");
}

settings {
  encodeUrl: true
  timeout: 0
}
