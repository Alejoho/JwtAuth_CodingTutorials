meta {
  name: Register
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/authentication/register
  body: json
  auth: inherit
}

body:json {
  {
    "username": "{{username}}",
    "password": "{{password}}",
    "email": "{{email}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
