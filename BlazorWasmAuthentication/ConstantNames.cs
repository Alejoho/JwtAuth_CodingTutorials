namespace BlazorWasmAuthentication;

public static class ConstantNames
{
    // Name for the named http client
    public const string ServerApiHttpClient = nameof(ServerApiHttpClient);

    // Name of the setting which holds the backend url
    public const string ServerUrl = nameof(ServerUrl);
}
